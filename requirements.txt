# Core (required to run the starter as-is)
streamlit
numpy

sentence-transformers   # real embeddings instead of TF-IDF
openai                  # LLM-generated answers via DeepSeek's OpenAI-compatible API
python-dotenv           # loads DEEPSEEK_API_KEY from .env

# Optional upgrades — install when you get there
# faiss-cpu               # or chromadb, for a real vector database at scale
# pypdf                    # if you want to ingest PDF documents
